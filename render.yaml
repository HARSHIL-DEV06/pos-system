services:
  - type: web
    name: pos-system
    env: python
    plan: free
    pythonVersion: 3.11
    buildCommand: pip install --only-binary :all: -r requirements.txt
    startCommand: gunicorn app:app